name: corridor-a3-apply

on:
  push:
    branches: [corridor-integration-sprint]

permissions:
  contents: write

jobs:
  apply:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: corridor-integration-sprint
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node tools/apply-corridor-a3-east.mjs
      - run: npm test
      - name: Commit generated East corridor data
        run: |
          if git diff --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+bpiper02@users.noreply.github.com"
          git add data/nodes.json public/data/nodes.json data/services.json public/data/services.json data/source/corridor-promotion-manifest-2026-09-12.json
          git commit -m "Promote East corridor local patterns"
          git push origin HEAD:corridor-integration-sprint
