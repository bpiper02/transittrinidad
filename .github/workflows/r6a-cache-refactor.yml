name: r6a-cache-refactor

on:
  push:
    branches: [r-hardening-open-source]

permissions:
  contents: write

jobs:
  apply:
    if: contains(github.event.head_commit.message, 'Apply R6a cache hardening')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node tools/apply-r6a-cache.mjs
      - run: npm install --no-save eslint@9.35.0
      - run: npm run check:public
      - run: npm run lint
      - run: npm test
      - name: Commit refactor
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/app-v2.js
          git diff --cached --quiet && exit 0
          git commit -m "Harden rider caches against invalid JSON shapes"
          git push origin HEAD:r-hardening-open-source
